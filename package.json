{
  "name": "ai-resume-checker-root",
  "private": true,
  "description": "Wrapper folder — the real app is in ./PlacementOS. Use scripts below or cd into that folder.",
  "scripts": {
    "install:app": "npm install --prefix PlacementOS",
    "install:server": "npm install --prefix PlacementOS/server",
    "install:all": "npm run install:app && npm run install:server",
    "dev": "npm run dev --prefix PlacementOS",
    "dev:all": "npm run dev:all --prefix PlacementOS",
    "api:dev": "npm run api:dev --prefix PlacementOS",
    "build": "npm run build --prefix PlacementOS",
    "typecheck": "npm run typecheck --prefix PlacementOS"
  }
}
